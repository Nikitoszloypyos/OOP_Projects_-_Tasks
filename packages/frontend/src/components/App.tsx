import '../styles/App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="app-placeholder">
        <span className="app-eyebrow">Task Management System</span>
        <h1>Project workspace is ready</h1>
        <p>Frontend dependencies are isolated and the app is ready for FSD structure.</p>
      </section>
    </main>
  )
}

export default App
